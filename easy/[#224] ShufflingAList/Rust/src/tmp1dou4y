extern crate rand;

use std::io;
use std::cmp::Ordering;
use rand::Rng;
use stf::env::ar

fn main(&mut list: &Vec<uint>)
{
	let n = 
	for x in list
	{
		let randNum = rand::thread_rnd().gen_range(1, 2);
		if
	}
	
}