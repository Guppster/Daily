use std::io;
use std::cmp::Ordering;
use std::env;

fn main()
{
	let args: Vec<_> = env::args().collect();

	if args.len() > 1
	{
		println!("There are(is) {} argument(", args.count())
	}
}//End of main