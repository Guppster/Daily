extern crate rand;

use std::io;
use std::cmp::Ordering;
use std::env;
use rand::Rng;

fn main()
{
	let args: Vec<_> = env::args().collect();
	let result: Vec<_> = Vec::new();

	if args.len() > 1
	{
		println!("There are(is) {} argument(s)", args.len() - 1)
	}

	for x in re
	{
		let secret_number = rand::thread_rng().gen_range(0, 101);
	}
}//End of main