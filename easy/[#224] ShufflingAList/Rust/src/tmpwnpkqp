extern crate rand;

use std::io;
use std::cmp::Ordering;
use rand::Rng;

fn main(list: Vec<int>)
{
	let result: Vec<i32> = Vec::new();
	for x in list
	{

	}
	let randNum = rand::thread_rnd().gen_range(1, n)
}