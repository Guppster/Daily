extern crate rand;

use std::io;
use std::cmp::Ordering;
use rand::Rng;

fn main(list: (i32, i32, i32, i32, i32))
{
	let n = list
	for x in list
	{
		let randNum = rand::thread_rnd().gen_range(1, 2);
		if
	}
	
}